# dependencies
node_modules/

# env (secrets)
.env

# runtime data — кеш та бекапи створюються автоматично при запуску
data/cache/
data/backups/

# uploads — користувацькі файли (студентські фото) лишаються локально
uploads/*
!uploads/.gitkeep

# тимчасові артефакти запису
data/items/*.tmp.json

# macOS / IDE
.DS_Store
.idea/
.vscode/

# тестові артефакти ручного тестування
import_test.json
image.png
_smoke.mjs
